import { ScrollViewProps } from 'react-native';
declare type props = {
    children?: any;
    barStyle?: 'light-content' | 'dark-content' | 'default';
    statusBarColor?: string;
    bottomSafeAreaColor?: string;
    scrollType?: 'none' | 'scroll' | 'keyboard';
    translucent?: boolean;
    scrollViewProps?: ScrollViewProps;
};
export declare const ScreenWrapper: ({ barStyle, statusBarColor, bottomSafeAreaColor, scrollType, translucent, scrollViewProps, children, }: props) => JSX.Element;
export {};
