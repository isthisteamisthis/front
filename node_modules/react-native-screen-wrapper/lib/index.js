import React, { Fragment } from 'react';
import { SafeAreaView, ScrollView, StatusBar, } from 'react-native';
import { KeyboardAwareScrollView } from 'react-native-keyboard-aware-scroll-view';
export const ScreenWrapper = ({ barStyle = 'light-content', statusBarColor = '#000', bottomSafeAreaColor, scrollType = 'none', translucent = false, scrollViewProps, children, }) => {
    return (React.createElement(Fragment, null,
        !translucent && (React.createElement(SafeAreaView, { style: { backgroundColor: statusBarColor } })),
        React.createElement(StatusBar, { translucent: translucent, barStyle: barStyle, backgroundColor: translucent ? 'transparent' : statusBarColor }),
        scrollType === 'scroll' ? (React.createElement(ScrollView, { ...scrollViewProps }, children)) : scrollType === 'keyboard' ? (React.createElement(KeyboardAwareScrollView, { ...scrollViewProps }, children)) : (children),
        bottomSafeAreaColor && (React.createElement(SafeAreaView, { style: { backgroundColor: bottomSafeAreaColor } }))));
};
//# sourceMappingURL=index.js.map